{
  "name": "juristum-tma",
  "version": "0.9.4",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "prestart": "node scripts/check-db-url.mjs",
    "start": "npx prisma db push --schema=prisma/schema.prisma && next start -p $PORT",
    "postinstall": "npx prisma generate --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/node": "^20.12.12",
    "@types/react": "^18.3.3"
  }
}
