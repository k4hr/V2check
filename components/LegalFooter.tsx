/* path: components/LegalFooter.tsx */
'use client';

/**
 * LegalFooter — заглушка.
 * Компонент существует в кодовой базе, но НИГДЕ не отображается.
 * Даже если передать force — вернёт null.
 */
export default function LegalFooter(_: { force?: boolean } = {}) {
  return null;
}
